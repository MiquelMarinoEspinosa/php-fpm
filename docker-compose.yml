version: '3'

services:
    php-fpm:
        build:
            context: etc/devel/docker/php-fpm
        image: php/php-fpm
        container_name: php-fpm
        volumes:
            - ".:/app"
        extra_hosts:
            - "docker.host:${DOCKER_HOST_IP}"
        tty: true